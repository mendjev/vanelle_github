name: Moon

on:
  workflow_dispatch:

jobs:
  moon-job:
    runs-on: ubuntu-latest
    steps:
      - name: Get Moon phase
        uses: wei/curl@v1
        with:
          args: "https://wttr.in/Moon?format=3"